import { Book, FileText, GitHub, Folder, Archive, Package, Globe, Hash, Rss, Icon } from 'react-feather';

export const IconMap = {
    Book, FileText, GitHub, Folder, Archive, Package, Globe, Hash, Rss
};
export type Icons = keyof typeof IconMap;